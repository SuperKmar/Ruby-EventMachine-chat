CREATE table "TASKS" (
    "ID"        SERIAL PRIMARY KEY,
    "PARENT"    INTEGER,
    "ROOM"   	INTEGER REFERENCES "ROOMS"("ID") NOT NULL,
    "NAME"   	CHAR(100),
    "COMPLEATION"  INTEGER,
    "ADDED_ON"   TIMESTAMP NOT NULL,
    "COMPLEATED_ON"   TIMESTAMP
);

ALTER TABLE "TASKS" 
	ADD FOREIGN KEY ("PARENT")
REFERENCES "TASKS"("ID");

