CREATE table "ROOMUSERS" (
    "ID"         SERIAL PRIMARY KEY,
    "ROOM"       INTEGER NOT NULL REFERENCES "ROOMS",
    "USER_ID"    INTEGER NOT NULL REFERENCES "USERS",
	CONSTRAINT unq_roomusers UNIQUE( "ROOM","USER_ID")
);